
struct Point{
		int x;
		int y;
};

struct Point newPoint(int x, int y){
        struct Point p;
        p.x = x;
        p.y = y;
        return p;
}

int main(void){
        struct Point p;

        p = newPoint(5, 6); //Nouveau Point
        print(p.x);
        print(p.y);

      return 0;
}
